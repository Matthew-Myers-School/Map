map is set at 32 levels, with an infinite amount of entries
I store the data as a Key_T variable and a Value_T variable, rather than a pair, so it most likely cannot be compared directly to a std map

